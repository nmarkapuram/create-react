# create-react
Create sample Project on React


# Redux - Toolkit
- Install Redux Toolkit (@reduxjs/toolkit), react-redux
- Build Our store
- Connect store to app
- Create a cartSlice
- dispatch (action)

#Setting Up Testing
- Inslal React Testing Library
- Install Jest
- Install Babel dependencies
- Configure Babel
- Change Parcel configuration to disable default babels transpilation
- Jest Config -> 
    npx jest --init (legacy)
    npm init jest@latest (new way which uses create-jest)
- Install jsdom library
    npm install --save-dev jest-environment-jsdom
- Install @babel/preset-react to make jsx work
    - npm i -D @babel/preset-react
    -  Add inside config
- Install @testing-library/jest-dom

it and test both are same

